name: fin_pulse
description: FIN PULSE mobile finance app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.2
  provider: ^6.0.0
  fl_chart: ^0.69.0

flutter:
  uses-material-design: true
